a
{{#if (and child.id child.save)}}
 b
  {{#unless makingGrandChild}}
  c
    {{#each grandChildren as |grandChild|}}
   d
      {{#if (and grandChild.id grandChild.save)}}
      e
        {{tetris-player parent=child isTopmostComponent=false model=grandChild}}
      {{/if}}
    {{/each}}
  {{/unless}}
{{/if}}
